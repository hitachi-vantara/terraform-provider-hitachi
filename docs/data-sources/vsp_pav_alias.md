---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "hitachi_vsp_pav_alias Data Source - terraform-provider-hitachi"
subcategory: ""
description: |-
  VSP PAV Alias: The following request obtains information about PAV aliases.
---

# hitachi_vsp_pav_alias (Data Source)

VSP PAV Alias: The following request obtains information about PAV aliases.

## Example Usage

```terraform
#
# Hitachi VSP PAV Alias Data Retrieval
#
# This section defines a data source block to fetch PAV alias information from a
# Hitachi Virtual Storage Platform (VSP) using HashiCorp Configuration Language (HCL).
#
# The data source block "hitachi_vsp_pav_alias" retrieves details about PAV aliases
# associated with the provided parameters. This enables you to access the list of
# PAV aliases returned by the storage system.
#
# Customize the values of the parameters (serial, cu_number, base_ldev_id, alias_ldev_ids)
# to match your environment. You can retrieve all PAV aliases, filter by a specific
# Control Unit (CU), filter by a base LDEV, or return only specific alias LDEVs.
#
# Parameters:
# - serial: (Required) Serial number of the storage system.
# - cu_number: (Optional) Control Unit number (0-255). If omitted, returns all PAV aliases.
# - base_ldev_id: (Optional) Filters alias entries whose base volume matches this LDEV.
# - alias_ldev_ids: (Optional) List of alias LDEV IDs.
#

data "hitachi_vsp_pav_alias" "all_pav_aliases" {
  serial = 12345
}

output "all_pav_aliases" {
  value = data.hitachi_vsp_pav_alias.all_pav_aliases.pav_aliases
}

# Filter by CU number
# data "hitachi_vsp_pav_alias" "cu2_pav_aliases" {
#   serial    = 12345
#   cu_number = 2
# }
#
# output "cu2_pav_aliases" {
#   value = data.hitachi_vsp_pav_alias.cu2_pav_aliases.pav_aliases
# }
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `serial` (Number) Serial number of the storage system

### Optional

- `alias_ldev_ids` (List of Number) Alias LDEV IDs. When set, the provider returns entries whose ldev_id matches any of the provided IDs.
- `base_ldev_id` (Number) Base LDEV ID. When set, the provider returns alias entries whose c_base_volume_id (or s_base_volume_id) matches this base LDEV.
- `cu_number` (Number) CU number (0-255). If omitted, returns all PAV aliases.
- `pav_aliases` (Block List) PAV alias information. (see [below for nested schema](#nestedblock--pav_aliases))

### Read-Only

- `id` (String) The ID of this resource.

<a id="nestedblock--pav_aliases"></a>
### Nested Schema for `pav_aliases`

Optional:

- `c_base_volume_id` (Number) LDEV number of the base device assigned within the storage system (only for alias devices)
- `cu_number` (Number) CU number
- `ldev_id` (Number) LDEV number
- `pav_attribute` (String) PAV attribute of LDEV (BASE or ALIAS)
- `s_base_volume_id` (Number) LDEV number of the base device set by the user (only for alias devices)
